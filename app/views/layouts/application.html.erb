<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ecommerce Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>

  </head>

  <body class="bg-gray-900">
    <!-- Flowbite Navbar -->
    <nav class="bg-white border-gray-200 dark:bg-gray-900 dark:border-gray-700 border-b">
      <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
          <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">DennCommerce</span>
        </a>
        <div class="flex items-center space-x-4">
          <div x-data="{ open: false }" class="relative">
            <button @click="open = !open" class="text-gray-700 dark:text-gray-200 hover:text-blue-600 dark:hover:text-blue-400 focus:outline-none" aria-label="Account">
              <!-- User/account icon -->
              <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 7.5a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.5 19.5a7.5 7.5 0 0115 0v.75a.75.75 0 01-.75.75h-13.5a.75.75 0 01-.75-.75V19.5z"></path></svg>
            </button>
            <div x-show="open" @click.away="open = false" class="absolute right-0 mt-2 w-40 bg-white dark:bg-gray-800 rounded shadow-lg z-50 py-2">
              <% if user_signed_in? %>
                <div class="px-4 py-2 text-gray-700 dark:text-gray-200 text-sm">Hi, <%= current_user.email %></div>
                <%= button_to 'Logout', logout_path, method: :delete, data: { turbo_method: :delete }, class: "block w-full text-left px-4 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 text-sm" %>
              <% else %>
                <%= link_to 'Login', login_path, class: "block w-full text-left px-4 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 text-sm" %>
                <%= link_to 'Sign Up', signup_path, class: "block w-full text-left px-4 py-2 text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 text-sm" %>
              <% end %>
            </div>
          </div>
          <%= link_to cart_path, class: "relative text-gray-700 dark:text-gray-200 hover:text-blue-600 dark:hover:text-blue-400", aria: { label: "Cart" } do %>
            <!-- Cart icon -->
            <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 3h1.386c.51 0 .955.343 1.087.835l.383 1.437m0 0L7.02 15.607a2.25 2.25 0 002.184 1.743h7.592a2.25 2.25 0 002.184-1.743l1.214-8.335H5.106zm0 0L5.25 6.75m0 0a.75.75 0 01.75-.75h12a.75.75 0 01.75.75m-2.25 12a1.5 1.5 0 11-3 0m3 0a1.5 1.5 0 11-3 0"></path></svg>
            <% if defined?(cart_item_count) && cart_item_count > 0 %>
              <span class="absolute -top-2 -right-2 bg-red-600 text-white text-xs font-bold rounded-full px-1.5 py-0.5 min-w-[20px] text-center"><%= cart_item_count %></span>
            <% end %>
          <% end %>
        </div>
      </div>
    </nav>
    <%# Flash messages %>
    <div class="fixed top-4 left-1/2 transform -translate-x-1/2 z-50 w-full max-w-md flex flex-col gap-2">
      <% flash.each do |key, value| %>
        <div class="flex items-center px-4 py-3 rounded shadow-lg transition-all animate-fade-in
          <%= key == 'notice' ? 'bg-green-600 text-white border-l-4 border-green-400' : 'bg-red-600 text-white border-l-4 border-red-400' %>">
          <svg class="w-5 h-5 mr-3 flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <% if key == 'notice' %>
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            <% else %>
              <circle cx="12" cy="12" r="10" /><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4m0 4h.01" />
            <% end %>
          </svg>
          <span><%= value %></span>
        </div>
      <% end %>
    </div>
    <main class="mx-auto px-5 bg-gray-900">
      <%= yield %>
    </main>
  </body>
</html>
